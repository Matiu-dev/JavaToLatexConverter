{
  "$id": "distance_cities_schema",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "A representation of a distance between cities",
  "title": "distance",
  "type": "object",
  "required": [
    "citya",
    "cityb",
    "distance"
  ],
  "definitions": {
    "citya": {
      "$comment": "name of city a",
      "type": "string"
    },
    "cityb": {
      "$comment": "name of city b",
      "type": "string"
    },
    "distance": {
      "$comment": "distance between cities",
      "type": "string"
    }
  },
  "properties": {
    "citya": {
      "$ref": "#/definitions/citya"
    },
    "cityb": {
      "$ref": "#/definitions/cityb"
    },
    "distance": {
      "$ref": "#/definitions/distance"
    }
  }
}